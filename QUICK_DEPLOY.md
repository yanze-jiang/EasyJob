# 🚀 快速部署指南 - 按顺序执行

## ⚡ 快速开始

### 步骤 1: 创建前端环境变量文件（手动操作）

由于权限限制，请手动创建 `frontend/.env.production` 文件：

```bash
cd frontend
echo "VITE_API_BASE_URL=/api" > .env.production
```

或者手动创建文件，内容：
```
VITE_API_BASE_URL=/api
```

### 步骤 2: 检查后端环境变量

打开 `backend/.env` 文件，确认：
- `JWT_SECRET` 不是默认值（如果是，运行 `openssl rand -base64 32` 生成）
- `DATABASE_URL` 指向正确的数据库
- `DASHSCOPE_API_KEY` 已配置
- `NODE_ENV=production`

### 步骤 3: 安装依赖

```bash
# 安装后端依赖
cd backend
npm install

# 安装前端依赖
cd ../frontend
npm install
```

### 步骤 4: 运行数据库迁移

```bash
cd backend
npm run migrate
```

### 步骤 5: 构建应用

```bash
# 构建后端
cd backend
npm run build

# 构建前端
cd ../frontend
npm run build
```

### 步骤 6: 验证构建

检查构建产物：
- `backend/dist/` 目录应包含编译后的文件
- `frontend/dist/` 目录应包含静态文件

### 步骤 7: 启动服务（测试）

```bash
# 启动后端（测试）
cd backend
npm start

# 在另一个终端启动前端预览
cd frontend
npm run preview
```

### 步骤 8: 部署到生产环境

参考 `DEPLOYMENT.md` 选择部署方式（PM2、Nginx、Docker等）

---

## 📞 需要帮助？

告诉我你当前在哪一步，我会协助你完成！

