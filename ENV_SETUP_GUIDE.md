# 环境变量配置指南

## 📝 步骤 1: 配置后端环境变量

### 1.1 检查现有配置

你的 `backend/.env` 文件已存在。请确保它包含以下必需配置：

```env
# 服务器配置
PORT=4000
NODE_ENV=production

# 数据库配置（必须指向生产数据库）
DATABASE_URL=postgresql://用户名:密码@主机:5432/easyjob

# JWT 密钥（⚠️ 必须更改！）
# 生成强随机密钥: openssl rand -base64 32
JWT_SECRET=你的强随机密钥

# LLM API 配置
DASHSCOPE_API_KEY=你的DashScope API密钥
LLM_MODEL=qwen-plus
```

### 1.2 生成 JWT 密钥

如果还没有强随机密钥，运行以下命令生成：

```bash
openssl rand -base64 32
```

将生成的密钥复制到 `JWT_SECRET` 配置项。

### 1.3 验证配置

打开 `backend/.env` 文件，确认：
- ✅ `DATABASE_URL` 指向正确的生产数据库
- ✅ `JWT_SECRET` 不是默认值（已更改为强随机字符串）
- ✅ `DASHSCOPE_API_KEY` 已配置
- ✅ `NODE_ENV=production`

## 📝 步骤 2: 配置前端环境变量

### 2.1 创建前端生产环境变量文件

创建 `frontend/.env.production` 文件：

```bash
cd frontend
cat > .env.production << EOF
VITE_API_BASE_URL=/api
EOF
```

或者手动创建文件，内容如下：

```env
# 如果前后端部署在同一域名
VITE_API_BASE_URL=/api

# 如果前后端部署在不同域名，使用：
# VITE_API_BASE_URL=https://api.yourdomain.com/api
```

### 2.2 验证配置

确认 `frontend/.env.production` 文件已创建并包含正确的 API URL。

## ✅ 配置完成检查清单

- [ ] `backend/.env` 文件存在且配置正确
- [ ] `JWT_SECRET` 已更改为强随机字符串
- [ ] `DATABASE_URL` 指向生产数据库
- [ ] `DASHSCOPE_API_KEY` 已配置
- [ ] `frontend/.env.production` 文件已创建
- [ ] `VITE_API_BASE_URL` 配置正确

## 🔒 安全提醒

1. **不要将 `.env` 文件提交到 Git**
2. **生产环境必须使用强随机 JWT_SECRET**
3. **数据库密码要足够强**
4. **API 密钥要妥善保管**

